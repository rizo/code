(jbuild_version 1)

(executable
  ((name Basic)
   (modules (Basic))
   (preprocess (pps (code.ppx)))
   (libraries (code code.ppx fmt))))

;(executable
;  ((name Log)
;   (modules (Log))
;   (preprocess (pps (code.ppx)))))

